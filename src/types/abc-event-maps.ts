import { AbcLocalEvent } from "./abc-events"

/**
 * Provides a _typing_ lookup service for incoming `AbcLocalEvent` events,
 * indicating what type the payload of the event will be.
 */
export interface AbcLocalEventMap {
  [AbcLocalEvent.set]: 
}